import styled from "styled-components";

const SlidesStyle = styled.div`
  .carousel-data {
    background: #ffc107de;
    font-size: 1.2rem;
    font-weight: 500;
    color: black;
  }
`;

export default SlidesStyle;
